@model List<TodoList>

<div class="container">
  <div class="row pt-4 pb-3">
    <div class="col-6">
      <h2 class="text-primary">
        Todo List
      </h2>
    </div>
    <div class="col-6 text-end">
      <a asp-controller="TodoList" asp-action="Create" class="btn btn-primary">
        Create New List
      </a>
    </div>
  </div>


  <h1> List </h1>
  <table class="table table-bordered table-striped">
    <thead>
      <tr>
        <th>
          List Name
        </th>
        <th>

        </th>
      </tr>
    </thead>

    <tbody>
      @foreach (var obj in Model.OrderBy(u => u.DisplayOrder))
      {
        <tr>
          <td>@obj.Name</td>
          <td>
            <div class="w-75 btn-group role="group">
              <a asp-controller="TodoList" asp-action="Edit" asp-route-id="@obj.Id" class="btn btn-primary mx-2">Edit</a>
              <a asp-controller="TodoList" asp-action="Delete" asp-route-id="@obj.Id" class="btn btn-danger mx-2">Delete</a>
            </div>
          </td>
        </tr>
      }
    </tbody>
  </table>

</div>